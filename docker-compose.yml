
services:
  salemanagement:
    build:
      context: .
      dockerfile: salemanagement/Dockerfile
    ports:
      - "9001:9001"
    networks:
      - salemanagement-networks
 
networks: 
  salemanagement-networks:
    driver: bridge